# Deployment files run on Linux — pin them to LF so a Windows checkout can't
# introduce CRLF that breaks the Docker build or container runtime.
deploy/** text eol=lf
.dockerignore text eol=lf
*.sh text eol=lf
